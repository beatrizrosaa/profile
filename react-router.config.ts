import type { Config } from "@react-router/dev/config";
// import { vercelPreset } from '@react-router/dev/presets/vercel';

export default {
  // Modo SPA: desabilita SSR para gerar build estático em dist
  ssr: false,
} satisfies Config;
